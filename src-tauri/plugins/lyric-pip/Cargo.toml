[package]
name = "tauri-plugin-lyric-pip"
version = "0.1.0"
edition = "2021"
links = "tauri-plugin-lyric-pip"
license = "AGPL-3.0"

[dependencies]
tauri = { version = "2", default-features = false }

[build-dependencies]
tauri-plugin = { version = "2", features = ["build"] }
